node_modules
.next
.git
harness/runs
*.log
.env
.env.*
!.env.example

# Operational state and secrets never enter the build context.
**/.env
**/.env.*
**/*.pem
**/*.key
**/*.pfx
**/*.sql
backups/
logs/
state/
releases/
**/[[]DATABASE]
.memsearch
.agents
.codex
.claude

# `astryx theme build` output — regenerated by `pnpm build` inside the image
# (see Dockerfile: RUN pnpm build), so the local copy is never needed as input.
src/shared/components/kt-xnk.js
src/shared/components/kt-xnk.d.ts
src/shared/components/theme.built.css
