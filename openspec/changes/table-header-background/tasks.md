# Tasks: Table Header Background Color

**Status:** all tasks complete (2026-09-12)

- [x] 1.1 Add `table-header` component override to `theme.js`
      (`--color-background-muted`).
- [x] 1.2 `pnpm theme:build` regenerates `theme.built.css` with the new
      rule.
- [x] 1.3 `./harness/verify.sh` passes.
- [x] 1.4 Live-verified in browser: contracts list header + nested
      Shipment table header both show the new background (see
      `harness/PROGRESS.md`, 2026-09-12 entry).
