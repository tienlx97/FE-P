# Tasks

- [x] 1. Query parsing + matching (`config/quick-search.js`, tests), search
  adapter (`api/quick-search.js`), `QuickSearchPalette` mounted in the
  protected layout behind `logistics:contracts:view`; run the full harness.
