# Tasks

- [x] 1. Split `MetaCommissionPanel` into summary / parties / tracking cards,
  share the view model (`useCommissionView`), add the commission detail page
  and route, slim the contract tab, point the list's "Xem" at the page;
  verify in-browser and run the full harness.
