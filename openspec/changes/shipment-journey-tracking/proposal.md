# Shipment journey tracking

Show the shipment's Incoterm journey on its detail page using the backend's resolved milestones. Let users confirm actual milestone dates and record each container's empty return for CIF. Keep the tracking rules in BE-kt-xnk so status, confirmations, and container returns produce one consistent journey.
