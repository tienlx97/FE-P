# Tasks: Default contracts list to Loại hợp đồng = Chính thức

## 1. Default filter

- [x] 1.1 Seed `filterConditions` with a `contractType Equals Official` condition — verify: `./harness/verify.sh` green.
