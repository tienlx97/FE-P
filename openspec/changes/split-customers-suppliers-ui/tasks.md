# Tasks

- [x] 1. Implement full Customer/Supplier dialogs, independent lists and API
  wiring; switch supplier consumers; update navigation/tests/docs; run the full
  harness and capture browser evidence.
- [x] 2. Wire the Suppliers list's "Xoá" button to BE-kt-xnk's new
  `DELETE /api/v1/suppliers/{id}` (`add-delete-supplier`) with a confirm
  dialog; run the full harness and capture browser evidence.
