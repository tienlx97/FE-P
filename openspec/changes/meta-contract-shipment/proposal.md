# Meta contract Shipment tab

Rebuild the Contract detail Shipment tab to match Figma frame `98:2343` in the existing Meta theme. Keep live Shipment data and create/edit behavior. Use Astryx sizing conventions and responsive card/table views.

The design's operational details are shown only when supported by the current Shipment, VGM, supplier and cost-category data. Fields absent from the API remain unrepresented.
